__merge__: ../../api/comp_control_method.yaml

name: negative_normal
label: negative_normal
summary: A negative control which generates normal distributed data.
description: |
  This control method generates normally distributed data as a negative control,
  using a fixed mean of 3 and standard deviation of 1.

resources:
  - type: r_script
    path: script.R

engines:
  - type: docker
    image: openproblems/base_r:1

runners:
  - type: executable
  - type: nextflow
    directives:
      label: [midtime, midmem, midcpu]